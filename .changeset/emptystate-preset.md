---
"@atlure/ui": minor
---

EmptyState gains an optional `preset` prop (`"no-results" | "no-filters" | "no-radius"`) plus an exported `DEFAULT_EMPTY_STATE_COPY` map, so callers can drop in default copy while still overriding individual strings.
